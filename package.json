{
	"private": true,
	"name": "ai-cost-usage-tracker",
	"version": "9.7.0",
	"description": "Local-first browser extension for tracking AI usage, cost, energy, and carbon across Claude, ChatGPT, Gemini, Mistral, Perplexity, Grok, Meta AI, and Microsoft Copilot.",
	"scripts": {
		"test": "node --test tests/unit/*.test.mjs",
		"test:unit": "node --test tests/unit/*.test.mjs",
		"test:harness": "bash tests/harness/smoke.test.sh",
		"test:e2e": "playwright test",
		"test:e2e:headed": "playwright test --headed",
		"test:e2e:install": "playwright install chromium",
		"audit": "node scripts/audit-debug-privacy.js && node scripts/check-dataclasses.js && node scripts/check-message-registry.js && node scripts/check-platform-coverage.js",
		"check:handlers": "node scripts/check-message-registry.js",
		"check:release-packages": "node scripts/check-release-packages.js",
		"verify": "python3 scripts/run_checks.py quick",
		"verify:all": "python3 scripts/run_checks.py all",
		"release": "node scripts/release-build.js all",
		"release:chrome": "node scripts/release-build.js chrome",
		"release:firefox": "node scripts/release-build.js firefox",
		"release:all": "node scripts/release-build.js all"
	},
	"vendoredDependencies": {
		"webextension-polyfill": {
			"version": "0.12.0",
			"file": "lib/browser-polyfill.min.js",
			"source": "https://github.com/nicolo-ribaudo/webextension-polyfill",
			"license": "MPL-2.0"
		},
		"gpt-tokenizer-o200k-base": {
			"file": "lib/o200k_base.js",
			"source": "https://github.com/nicolo-ribaudo/gpt-tokenizer",
			"license": "MIT",
			"note": "UMD bundle of GPTTokenizer_o200k_base; version not embedded in minified output"
		}
	},
	"devDependencies": {
		"@playwright/test": "^1.59.1",
		"web-ext": "^10.1.0"
	}
}
